PORT=3001
# Comma-separated list of allowed origins, or * for any
CORS_ORIGIN=*
# Where the SQLite file lives
DB_PATH=./data/events.db
# Optional. If set, DELETE endpoints require: Authorization: Bearer <ADMIN_TOKEN>
ADMIN_TOKEN=
