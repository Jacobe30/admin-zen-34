{
  "name": "tameeni-events-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone Node/Express + Socket.IO backend that stores events and broadcasts them to the admin dashboard.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "socket.io": "^4.8.1"
  }
}
